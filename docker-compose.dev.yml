# пример запуска скрипта docker compose -f docker-compose.dev.yml -p nextjs-dev up

services:
  nextjs-dev:
    build:
      context: .
      dockerfile: ./docker/dev/Dockerfile.dev
    container_name: dev-mod
    ports:
      - "3001:3001"
    volumes:
      - ./:/app
      - /app/node_modules
