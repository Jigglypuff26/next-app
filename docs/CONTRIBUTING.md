# Руководство по внесению вклада

Спасибо за интерес к проекту! Мы рады вашему участию.

## Начало работы

1. Форкните репозиторий
2. Клонируйте ваш форк: `git clone https://gitflic.ru/your-username/next-app.git`
3. Создайте ветку для вашей функции: `git checkout -b feature/amazing-feature`
4. Установите зависимости: `npm i`

## Процесс разработки

### Запуск проекта

```bash
# Разработка (локально)
npm run dev

# Разработка (Docker)
npm run docker:dev
```

### Проверка кода

Перед коммитом убедитесь, что код соответствует стандартам:

```bash
# Проверка линтера и форматирования
npm run check

# Автоматическое исправление
npm run lint:fix
npm run format
```

### Архитектура проекта (FSD)

Проект использует архитектуру **Feature-Sliced Design**. Ознакомьтесь с [FSD_IMPROVEMENTS.md](./FSD_IMPROVEMENTS.md) для понимания структуры проекта.

Основные правила:

- **app/** - инициализация приложения, роутинг, провайдеры
- **widgets/** - крупные самостоятельные блоки интерфейса
- **features/** - функциональные возможности
- **entities/** - бизнес-сущности
- **shared/** - переиспользуемые части (UI компоненты, утилиты, конфигурация)

#### Правила импорта

- Слои могут импортировать только из нижележащих слоев
- Используйте публичный API через `index.ts` в каждом модуле
- Не создавайте циклических зависимостей

### Коммиты

Используйте понятные сообщения коммитов:

```
feat: добавлена новая функциональность
fix: исправлена ошибка
docs: обновлена документация
style: изменение форматирования
refactor: рефакторинг кода
test: добавлены тесты
chore: обновление зависимостей
```

### Pull Request

1. Убедитесь, что все проверки проходят (`npm run check`)
2. Добавьте описание изменений в PR
3. Укажите связанные issues (если есть)
4. Дождитесь code review

## Стандарты кода

- **TypeScript** - используйте строгую типизацию
- **ESLint** - следует правилам из `eslint.config.mjs`
- **Prettier** - автоматическое форматирование включено
- **Husky** - pre-commit hooks проверяют код перед коммитом

## Вопросы?

Если у вас есть вопросы, создайте issue в репозитории.
